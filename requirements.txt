# Install the project and all its dependencies from pyproject.toml.
# This file exists only for convenience: pip install -r requirements.txt
# Do NOT add packages here — edit pyproject.toml instead,
# then run: bazel run //:requirements.update
-e .
